# `apps` Folder

This folder currently stores **legacy/static milestone artifacts** used as migration sources.

Current milestone architecture uses:
- `uis/` for Next.js frontend applications.
- `services/` for API/backend implementations.

- **Main purpose**: preserve previous milestone deliverables while they are migrated.
- **Recommendation**: avoid adding new runtime applications here unless explicitly required by a milestone.
