# API Placeholder

This directory is reserved for upcoming API implementations.

Suggested future structure:
- `services/api/public/` for public endpoints
- `services/api/internal/` for internal/backoffice endpoints
- `services/api/shared/` for API-layer shared utilities
