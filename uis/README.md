# uis

This folder contains Next.js + TypeScript frontend applications.

## Applications
- `website`: public corporate website (home route `/`).
- `backoffice`: internal operational interface (home route `/`).

## Boundary
- Reuse business logic from canonical TypeScript modules in `src/`.
- Keep backend/API code in `services/`.
